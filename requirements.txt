# The contract has no runtime dependency outside GenVM. These are the
# exact toolchain versions the Direct Mode suite, the GenVM linter gate and
# the StudioNet integration suite were verified against; CI installs from
# requirements-test.txt, which includes this file.
genlayer-py==0.16.3
genlayer-test==0.29.2
genvm-linter==0.11.0
